#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../activepush/bin/example-client" "$@"
  ret=$?
else 
  coffee  "$basedir/../activepush/bin/example-client" "$@"
  ret=$?
fi
exit $ret
